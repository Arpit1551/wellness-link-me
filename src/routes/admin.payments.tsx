import { createFileRoute } from "@tanstack/react-router"; import { AdminShell } from "@/components/admin-shell"; import { AuthGuard } from "@/components/auth-guard"; import { StatusBadge } from "@/components/status-badge"; import { AdminLoading } from "@/components/admin-loading"; import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from "@/components/ui/table"; import { useClinicData } from "@/hooks/use-clinic-data";
type Payment={id:string;appointment_id:string;razorpay_payment_id:string|null;amount:number;currency:string;status:string;created_at:string};
export const Route=createFileRoute("/admin/payments")({head:()=>({meta:[{title:"Payments | BrightSmile Admin"}]}),component:Payments});function Payments(){const {data,loading}=useClinicData<Payment>("payments");return <AuthGuard><AdminShell title="Payments" subtitle="Track consultation transactions"><section className="overflow-hidden rounded-2xl border border-border bg-background">{loading?<AdminLoading/>:<Table><TableHeader><TableRow><TableHead>Payment ID</TableHead><TableHead>Appointment</TableHead><TableHead>Amount</TableHead><TableHead>Date</TableHead><TableHead>Status</TableHead></TableRow></TableHeader><TableBody>{data.map(p=><TableRow key={p.id}><TableCell className="font-mono text-xs">{p.razorpay_payment_id??"Pending"}</TableCell><TableCell className="font-mono text-xs">{p.appointment_id.slice(0,8)}…</TableCell><TableCell className="font-semibold">₹{p.amount.toLocaleString()}</TableCell><TableCell>{new Date(p.created_at).toLocaleDateString()}</TableCell><TableCell><StatusBadge status={p.status}/></TableCell></TableRow>)}{!data.length&&<TableRow><TableCell colSpan={5} className="h-32 text-center text-muted-foreground">No transactions yet.</TableCell></TableRow>}</TableBody></Table>}</section></AdminShell></AuthGuard>}